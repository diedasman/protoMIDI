# ZMK Config

`boards/shields/protomidi` contains the v1.0 protoMIDI shield definition.
The configuration supports the custom PCB's 3x4 switch matrix, two EC11
encoders, and eight always-on illuminated button LEDs. It intentionally has no
display configuration.

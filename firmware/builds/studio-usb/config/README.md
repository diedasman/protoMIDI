# USB Studio ZMK Config

This build matches the standard protoMIDI behavior and adds runtime keymap
editing through ZMK Studio over USB. Its physical layout contains the two
encoder push switches and eight illuminated push buttons; encoder rotation
bindings remain defined in the keymap source.

# USB Studio ZMK Config

This is the supported protoMIDI v1.1 firmware configuration. It provides
runtime keymap editing through ZMK Studio over USB. Its physical layout contains
the two encoder push switches and eight illuminated push buttons; encoder
rotation bindings remain defined in the keymap source.
